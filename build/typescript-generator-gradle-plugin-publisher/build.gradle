
plugins {
    id 'com.gradle.plugin-publish' version '1.0.0'
    id 'java-library'
}

group = 'com.github.kkuegler'

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    api "com.github.kkuegler:typescript-generator-gradle-plugin:${version}"
    api "com.github.kkuegler:typescript-generator-spring:${version}"
}

task printVersion {
    doLast {
        println "Releasing 'com.github.kkuegler.typescript-generator' Gradle plugin version ${project.version}..."
    }
}

pluginBundle {
    website = 'https://github.com/kkuegler/typescript-generator'
    vcsUrl = 'https://github.com/kkuegler/typescript-generator'
    tags = ['java', 'kotlin', 'json', 'typescript', 'typescript-generator', 'jackson', 'jakarta', 'rest', 'client', 'spring']
}

gradlePlugin {
    plugins {
        typescriptGeneratorPlugin {
            id = 'com.github.kkuegler.typescript-generator'
            displayName = 'Typescript-generator Gradle plugin'
            description = 'Generates TypeScript from Java - JSON declarations, REST service client'
            implementationClass = 'cz.habarta.typescript.generator.gradle.TypeScriptGeneratorPublishedPlugin'
        }
    }
}
